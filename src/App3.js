import React from 'react'

export default function App3(props) {
    // console.log(props.names);
    // console.log(props.marks);
    console.log(props.students);
  
    return(
        <div>check console rey 🥱🥱</div>
        )
}
